console.log("script caricato");

const OreLav = [40, 35, 45, 38, 42];

for (let i = 0; i < OreLav.length; i++) {
    let = paga = 0;
    if (OreLav[i] > 40) {
        paga = (15 * 40) + (20 * (OreLav[i] - 40))
        console.log("la paga di", i, "è", paga);
    } else {
        paga = 15 * OreLav[i];
        console.log("la paga di", i, "è", paga);

    }
}